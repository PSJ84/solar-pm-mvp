# .github/workflows/keep-alive.yml
name: Keep Railway Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 5분마다

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -s https://solar-pmapi-production.up.railway.app/health
